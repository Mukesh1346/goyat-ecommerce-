/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "localhost",
      "picsum.photos",
      "api.goyattrading.shop",
      "https://",
      "http://",
    ],
  },
};

export default nextConfig;
